<style>
  section.overlay.cover.fullscreen {
    min-height:500px!important;
  }
</style>

<section class="cover fullscreen image-bg overlay">
  <div class="background-image-holder">
    <%= image_tag "home5.jpg", class:"background-image", alt:"image" %>
  </div>
  <div class="container v-align-transform">
    <div class="row">
      <section>
        <div class="col-md-4 col-md-offset-4 col-sm-8 col-sm-offset-2">
          <div class="feature bordered text-center">
            <h4 class="uppercase">Unlock Here</h4>
            <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { class: 'text-left', method: :post }) do |f| %>
              <%= devise_error_messages! %>

              <%= f.email_field :email, placeholder: "Email", autofocus: true %>

              <%= f.submit "Resend Unlock Instructions" %>

              <hr />
              <%= render "accounts/shared/links" %>
            <% end %>
          </div>
        </div>
      </section>
    </div>
  </div>
</section>
